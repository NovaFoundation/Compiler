package "compiler/engines"

import "compiler/Compiler"

[Abstract]
class CodeGeneratorEngine {
    [Visible] Compiler controller
    
    [Public] [Abstract]
    writeFiles()
    
    /// Insert the main method into the correct file. Also set up the
    /// initialization for the program within the main method.
    [Public] [Abstract]
    insertMainMethod()
}